# Фронтенд кулинарного приложения (Django templates)

Клиентская часть приложения реализована на Django‑шаблонах с использованием HTML, CSS и JavaScript.  
Интерфейс предоставляет ленту рецептов, форму создания постов, детальный просмотр, профиль пользователя, авторизацию и регистрацию.

---

## Структура фронтенда

### Шаблоны

- `templates/base.html`  
  Базовый шаблон:
  - подключает общий CSS (`static/css/style.css`);
  - подключает общий JS (`static/js/script.js`);
  - содержит фиксированную шапку с логотипом, ссылками «Главная», «Войти/Профиль», «Админ панель»;
  - содержит футер с иконками «Главная», «Создать», «Войти» (отображается на мобильных).

- `templates/posts/home.html`  
  Главная страница:
  - строка поиска по тексту рецептов и имени автора;
  - кнопка «+ Создать пост»;
  - лента постов с:
    - аватаром автора;
    - именем пользователя;
    - первым изображением поста (если есть);
    - текстом рецепта или его превью;
    - ссылкой «Посмотреть рецепт полностью»;
    - лайками/дизлайками с количеством;
    - блоком комментариев и формой добавления комментария (для авторизованных).

- `templates/posts/create_post.html`  
  Страница создания поста:
  - большая `textarea` для ввода текста рецепта;
  - скрытый `<input type="file" multiple>` для изображений;
  - кнопка «+ Добавить изображение», которая открывает диалог выбора файлов;
  - кнопка «Опубликовать пост».

- `templates/posts/post.html`  
  Детальная страница отдельного рецепта:
  - галерея всех изображений поста;
  - название (обрезка текста поста до 40 символов);
  - полный текст рецепта.

- `templates/users/profile.html`  
  Профиль пользователя:
  - аватар (или дефолтный);
  - статус «Онлайн» / «Оффлайн» и время последнего посещения;
  - имя пользователя;
  - количество постов;
  - форма изменения аватара (для владельца профиля);
  - кнопка «Скачать мои данные» (выгрузка CSV с данными пользователя);
  - ссылка «Выйти»;
  - блок истории комментариев пользователя с датой и ссылкой на пост.

- `templates/users/auth.html`  
  Страница входа:
  - форма логина (логин + пароль);
  - вывод ошибок валидации;
  - ссылка на страницу регистрации.

- `templates/users/reg.html`  
  Страница регистрации:
  - форма регистрации (логин, email, телефон, пароль);
  - блок ошибок валидации;
  - чекбокс согласия с пользовательским соглашением/политикой;
  - ссылка на страницу входа.

---

## Статические файлы

### CSS

- `static/css/style.css`  

  Отвечает за:
  - общую типографику и подключение кастомных шрифтов (`El Messiri`, `Firenight`, `ArthurPen`);
  - фиксированную шапку и адаптивный футер;
  - оформление ленты постов (аватар, картинка, текст, иконки, счётчики);
  - оформление форм логина, регистрации, создания поста, профиля;
  - ресайз и адаптивную верстку для разных ширин экрана (`@media` для `<480px`, `481–960px`, `961–1200px`);
  - стили для кнопок (лайк/дизлайк, «Создать пост», «Добавить изображение», «Скачать мои данные» и т.п.);
  - стили для страниц поста и истории комментариев.

### JavaScript

- `static/js/script.js`  

  Клиентская логика:

  - Обработка кнопки «+ Добавить изображение»:
    - по клику вызывает `.click()` на скрытом `<input type="file" id="id_images">`.

  - Переключение видимости блока комментариев:
    - клики по `.comment-toggle` показывают/скрывают `.comments-block` внутри поста.

  - Обработка лайков и дизлайков:
    - клики по `.react-btn` отправляют `POST`‑запрос на `/<postId>/react/` с `value=like` или `value=dislike`;
    - используется CSRF‑токен, читаемый из cookies;
    - ответом приходит JSON со счётчиками и реакцией пользователя;
    - функция `updateReactions(post, data)` обновляет:
      - числовые значения лайков/дизлайков;
      - символы иконок (`♥/♡`, `🖤/💔`) в зависимости от текущей реакции.

  - Поиск по рецептам и пользователям:
    - поле ввода `.search-input`;
    - клик на `.search_icon` или нажатие Enter:
      - берётся текст из `searchInput.value`,
      - происходит переход на `/?q=<запрос>`;
    - backend фильтрует посты по тексту рецепта и имени автора.

---

## Как работает фронтенд вместе с backend

- Все страницы рендерятся на сервере через Django‑шаблоны.
- Дополнительные действия выполняются на клиенте через JavaScript:
  - лайки/дизлайки — асинхронно, без перезагрузки страницы;
  - показ/скрытие блоков комментариев;
  - отправка поискового запроса и переход на URL с параметром `q`.
- Стили обеспечивают адаптивность:
  - десктоп: шапка + центрированная лента постов;
  - планшет и мобильный: футер‑навигация, изменённые размеры аватара, textarea, карточек постов и комментариев.

---

## Запуск фронтенда

Фронтенд обслуживается самим Django‑приложением, отдельного сервера для него не требуется.

1. Установить зависимости и выполнить миграции (см. основной `README.md` для backend части).
2. Запустить сервер:

